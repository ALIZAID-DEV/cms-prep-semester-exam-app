# Subject
Algorithms & Flowcharts

# Topic
Introduction to Algorithms and Flowcharts — Lecture #05 (Yasir Yousuf)

# Confidence
High (0.88)

# Lecture Explanation
Ye lecture slide-set ek introductory overview hai of "Algorithm" aur "Flowchart". Roman-Urdu + English summary:

- Algorithm kya hai: A step-by-step recipe to solve a problem. Har algorithm mein hona chahiye: Input, Output, Clear Steps, Finite (stop kare), aur Doable (har step simple ho).
- Real-life examples diye gaye: making chai (step-by-step), ordering food on Foodpanda (includes a decision: total amount > Rs.500).
- Programming examples (algorithm-to-code mapping): Example for "Add two numbers" with steps and C++ code; ek aur example "Check even or odd" diya hai, lekin note: code snippet appears to be incorrectly repeated from add-two example (see Unclear or Missing Information).
- Flowchart kya hai: A picture-based representation of an algorithm using shapes (Start/End oval, Input/Output parallelogram, Process rectangle, Decision diamond). Flowcharts ko use karne ki wajahain: visual, language-independent, easy to share.
- Flowchart rules: One Start and One End, Top-to-Bottom flow, Clear arrows, No crossed lines ideally, Simple language.
- Complete examples as flowcharts/algorithms: pizza bill calculation, exam pass/fail check, find largest of two numbers.
- Quick tips: Don’t skip steps, don’t be vague, don’t overcomplicate; plan before drawing; use standard symbols; test with examples.
- Practice assignment: Algorithm + flowchart for checking if a student can afford a phone (compare savings vs price; show message or additional amount needed).
- Additional practice problems: ATM withdrawal logic and Temperature check logic.

Note: Yeh document slide-style outline hai — kuch slides likely diagrams/symbol images jin ki text capture missing hai. See "Unclear or Missing Information" below.

# Key Exam Points
(Use Roman Urdu + English; technical terms in English)

- Algorithm ki definition: step-by-step procedure to solve problem — must have Input, Output, Clear Steps, Finite, Doable.
- Input/Output: Algorithm may have zero ya zyada inputs; at least one output expected.
- Control structures basics: Sequence, Decision (if/else), Repetition (loops not shown explicitly in slides but concept implied).
- Example algorithms to yaad rakhain: Add two numbers (input two numbers, compute sum), Check even/odd (check remainder mod 2), Compare two values (find larger).
- Flowchart basic symbols (important for exams):
  - Start/End: OVAL
  - Input/Output: PARALLELOGRAM
  - Process/Calculation: RECTANGLE
  - Decision/Question: DIAMOND
- Flowchart rules (exam-style): One Start and One End; use arrows to show flow; avoid crossed lines; top-to-bottom flow preferred; use simple clear instructions.
- Example flowchart problems to practice: pizza-bill (multiply quantity × price), pass/fail check (marks >= 50), largest of two, ATM withdrawal (check balance >= amount), temperature ranges (hot/cold/pleasant).
- Good practices: Plan before coding; use standard symbols; test with sample data; keep each symbol instruction atomic (one action).
- Assignment logic outline (phone affordability): Input phone_price, input savings; if savings >= phone_price → output “You can buy it!” else output (phone_price − savings) as additional required amount.
- Recognize mismatches in lecture materials (e.g., code vs description) and be prepared to correct them in exam answers.

# MCQs
1) What is an essential property of an algorithm?
a. It must be written in English only  
b. It must be finite — end after a specific number of steps  
c. It must use diagrams only  
d. It must run on a computer to be valid  
Correct answer: b

2) Which flowchart symbol is used for a decision/question?
a. Rectangle  
b. Parallelogram  
c. Diamond  
d. Oval  
Correct answer: c

3) In the "Check even or odd" algorithm, which operation is used to determine evenness?
a. Multiplication by 2  
b. Subtraction of 2  
c. Modulo (remainder) with 2  
d. Division and comparing quotients  
Correct answer: c

4) According to the slides, which rule is recommended for a good flowchart?
a. Use multiple start points for flexibility  
b. Flow should generally move from top to bottom  
c. Avoid using standard symbols — create your own  
d. Do not use arrows to reduce clutter  
Correct answer: b

5) In the Foodpanda ordering example, which programming construct is implied by "If total amount > Rs. 500 Proceed to checkout Else Add more items"?
a. Loop  
b. Function call  
c. Decision (if/else)  
d. Recursion  
Correct answer: c

# Unclear or Missing Information
- Document nature: This appears to be a slide deck / outline rather than full prose — several slides likely contained images/diagrams that were not captured in the text.
- Slide "EDUCATION FOR FLOWCHART SYMBOLS 8" (page 8) shows heading but no list or images of symbols in the text capture — actual symbol diagrams appear missing.
- On the page for "FLOWCHART SYMBOLS" and later "KEY POINTS TO REMEMBER" there are placeholders/letters (a b c e f g h) and shape names (OVAL, PARALLELOGRAM, RECTANGLE, DIAMOND) but the associated images/labels or descriptions are incomplete or misaligned.
- Programming example "EXAMPLE-2: CHECK EVEN OR ODD NUMBER" includes C++ code that matches the "Add two numbers" example (variables num1, num2, sum and sum calculation). This is a mismatch between described algorithm (even/odd) and code shown — source text seems inconsistent.
- Several slides appear to have formatting artifacts (form feeds, page numbers, and blank lines). Possible missing flowchart diagrams for examples (pizza bill, pass/fail, largest of two) are not present in the text.
- If any diagrams, images, or slide notes were present in the original file (shapes, arrows, example flowcharts), they are not included in this text dump.